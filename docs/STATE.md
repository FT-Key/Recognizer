# Estado — Recognizer

- **Fase actual:** etapa 23 (OCR) completada; siguiente 25 (EPP o inventario según roadmap)
- **Rama:** `dev`
- **Actualizado:** 2026-09-22

## Hecho
- Repo `FT-Key/Recognizer` (`main`/`dev`); uv + Python 3.12; deps (+`ultralytics`).
- Etapas 0-8: core base, cámara/CLI `smoke`, manos+gestos, acciones locales (media/hotkey/command/script), puntero virtual, gestos personalizados/compuestos y enlaces.
- Etapas 9-9c: web React + `.exe` PyInstaller + `/health`; navegador CDP (`open_tab`/`tab_seek`/`tab_press`) y scroll `Victory`/`Closed_Fist`.
- Etapas 10a-10d: launcher multi-app vintage (`MenuTheme`, tkinter, `ESC`/`q`), contador YOLO (`yolo26n.pt`) + tracking ByteTrack y línea con banda muerta.
- Etapa 11: anti-intrusos (`intrusion.py`, puerto `AlertSink`, zona + alerta edge-triggered).
- Etapas 12-12b: postura YOLO pose (`yolo26n-pose.pt`), calibración por mediana y HUD "Calibrando".
- Etapa 15a: enrolamiento + login facial local (`data/faces/`); `FaceRepository`, InsightFace `buffalo_s` CPU.
- Etapa 15b: roles `admin > operator > viewer` (`identity.py` + `PolicyEngine`, `IdentityProvider` con `session.json`).
- Fixes 15b: submenú vintage + selector cámara + marco objetivo, overlay legible, cierre `wait_window`, `session.json` ignorado, latencia (búfer 1 + frame skipping), captura asíncrona (`LatestFrameSource` + worker), doble apertura y coste (`allowed_modules`, `max_inference_fps`).
- Etapa 15c: gestión de usuarios/accesos; fotos (`<id>.png`), `AccessEvent` + `AccessLogRepository` (`data/access/logins.jsonl`), paneles admin Usuarios y Accesos.
- Fixes 15c: redirección con cuenta regresiva, visor modal, formulario coherente, submenú rediseñado, "Cerrar sesión" rojo con confirmación.
- Release **v0.2.0** en GitHub (launcher + contador/anti-intrusos/postura + facial con roles); `docs/RELEASE-v0.2.0.md`.
- Etapa 15d: clave de respaldo por usuario. `core/domain/credentials.py` (PBKDF2-HMAC-SHA256, 600k iter, sal 16B, `compare_digest`, dummy hash anti-enumeración), `EnrolledFace.password_hash` (legado `""`), `FaceAuthConfig.min_password_length` (4), enrolamiento pide clave+confirmación, login sin cámara (`run_face_login_password` + `face_password_gui.py`), panel de usuarios con columna "Clave".
- Gate 15d verde: lint OK, mypy strict 206, pytest **1250 passed** (95.57%), check-arch 3/3, smoke 17.0 FPS. Reviewer: apta (hallazgos menores aplicados). Merge `0d5ee24` en `dev` (push OK).
- Etapa 15d-accesos-clave: `AccessEvent` con `method` (`facial`/`clave`) y `success` (legado → facial exitoso); el login con clave exitosa y los intentos fallidos quedan registrados; tabla con Método/Resultado en verde/rojo y detalle con banner ("Inicio con clave exitoso/fallido", "Inicio facial exitoso") y nota de falta de foto con clave.
- Etapa 15d-login-dni-ux: login solo por ID (`F-0001`) o DNI (el nombre ya no autentica; homónimos); `EnrolledFace.national_id` (7-8 dígitos, único, legado `""`); Enter envía el diálogo de clave y el fallo muestra error rojo con reintento; enrolamiento y CRUD validan con mensajes visibles. Gate verde (1281 passed, 95.63%). Reviewer: apta.
- Etapa 15d-intento-id: el acceso guarda lo ingresado (`attempted`, nunca la clave) y el motivo (`ID/DNI desconocido` vs `clave incorrecta`); tabla y detalle lo muestran en rojo; IDs nuevos aleatorios `F-A3F9` no secuenciales (legado válido). Gate verde (1284 passed, 95.58%). Reviewer: apta.
- Etapa 16: asistencia (`assistance`). `AssistanceMonitor` (dominio puro, debounce confirm/release), `raised_arms` (muñeca sobre hombro, `raise_margin: 0.05`, `required_arms: 2` configurable a 1). Runner `run_assistance` con una sola vía de inferencia (reutiliza `yolo26n-pose.pt` + `PoseEstimator`), alerta edge-triggered. Overlay con brazos hombro-codo-muñeca, resaltado del lado levantado, HUD y banner. `config.yaml` con `AssistanceConfig`. Gate verde (lint, mypy strict, 80/80 targeted pass, check-arch). Pendiente smoke con cámara real.
- Etapa 18: conteo de autos (`vehicle_counter`). `VehicleCounterConfig`, `AppId.VEHICLE_COUNTER` (`implemented=True`), runner `cli/apps/vehicle_counter.py`, overlay `adapters/overlay_vehicle.py`, import perezoso en `menu.py`, sección `vehicle_counter` en `config.yaml`. Reutiliza `LineCrossingCounter` + `UltralyticsDetector`. Tests en `tests/unit/test_vehicle_counter.py` (17 casos). Gate verde (lint, typecheck, suite completa y check-arch).
- Etapa 19: desenfoque de privacidad (`privacy_blur`). Dominio puro `core/domain/privacy.py` (`PixelRect`, `face_blur_regions`, `effective_blur_kernel`), puerto `FaceDetector`, adaptador `InsightFaceFaceDetector` (`buffalo_s`, solo módulo `detection`, sin embeddings), overlay con HUD "Rostros difuminados", runner `run_privacy_blur` con import perezoso. Gate verde (1417 passed, 94.48%, check-arch 3/3, smoke 8.3 FPS, corrida real 4.9 FPS). Reviewer: apta. Merge `c96d5b7` en `dev` (push OK).
- Etapa 21: edad y género (`gender_age`). Dominio `face_attributes.py` con `AgeGenderSmoother` (mediana edad + voto género por IoU), puerto `FaceAttributeEstimator`, adaptador `InsightFaceAttributeEstimator` (módulos `detection`+`genderage`), overlay `M/F/? ~edad`, `GenderAgeConfig`, runner perezoso. Gate verde (1472 passed, 94.68%, check-arch 3/3). Reviewer: apta con cambios menores. Etapa 20 salteada a pedido del usuario.
- Etapa 20: caídas (`fall_detector`). Dominio puro `core/domain/fall.py` (`FallDetector`, debounce confirm/release, `measure_fall` con bbox de torso vía hombros+caderas/rodillas), algoritmo: aspect ratio (alto/ancho < 0.8), centro Y (> 0.6), quietud (varianza < 0.02). Constants `DEFAULT_FALL_*`, config `FallDetectorConfig` + `FallDetectorAlertConfig`, overlay `adapters/overlay_fall.py` (esqueleto + HUD + banner), runner `cli/apps/fall_detector.py` (YOLO pose + alerta sonora `winsound`). Gate verde (1498 passed, 94.72%, lint, typecheck, check-arch 3/3).
- Etapa 22: somnolencia (`drowsiness`). Dominio puro `core/domain/drowsiness.py` (`DrowsinessDetector`, debounce confirm/release, `measure_drowsiness` con cabeza caida/nariz-hombros + cabeceo/oscilacion de nariz). Constants `DEFAULT_DROWSINESS_*`, config `DrowsinessConfig` + `DrowsinessAlertConfig`, overlay `adapters/overlay_drowsiness.py` (esqueleto + HUD + banner), runner `cli/apps/drowsiness.py` (YOLO pose + alerta sonora `winsound`). Gate verde (1525 passed, 94.73%, lint, typecheck, check-arch 3/3).
- Fix somnolencia (22): las señales faciales (EAR ojos cerrados, MAR bostezo) solo se evaluaban si había pose, así que sin ver el cuerpo/brazos la app no detectaba nada. `measure_drowsiness` acepta `pose=None` y `DrowsinessDetector.update` evalúa la cara de forma independiente de la pose; `people` cae a `len(faces)` si no hay poses. Tests face-only añadidos (1571 passed, 93.96%).
- Etapa 23: OCR en vivo (`ocr_reader`). Dominio puro `core/domain/ocr.py` (`OCRBox`, `ROI`, `OCRSnapshot`), puerto `OCREngine` (protocol con `NDArray[np.uint8]`), adaptador `EasyOCREngine` (`easyocr` GPU/CPU, lazy init, módulo `detection`), overlay `adapters/overlay_ocr.py` (bounding boxes + texto detectado), runner `cli/apps/ocr_reader.py` (ExitStack, camara real, `field(default_factory)`, `engine.open()`/`close()`). `OCRReaderConfig` con ROI normalizado 0..1, `process_every_n_frames`, `min_confidence`, `max_results`, `languages`. Tests: 28 casos (dominio, config, adaptador fake, overlay, menu lazy import, runner headless/quit/ESC/max_frames). Gate verde (1558 passed, 93.95%, lint, typecheck, check-arch 3/3).
- Fix OCR/cámara (23): `easyocr` arrastraba `opencv-python-headless` (sin GUI) y tapaba a `opencv-python`, así que `cv2.imshow` no existía y las apps no mostraban ventana. `pyproject.toml`: `[tool.uv] exclude-dependencies = ["opencv-python-headless", "opencv-contrib-python"]` y `lap>=0.5.12` como dep directa (ByteTrack de ultralytics). Verificado `cv2.imshow` + cámara y smoke con ventana.
- Fix OCR/rendimiento (23): la inferencia corría en el hilo del bucle y congelaba la ventana; EasyOCR en CPU tardaba ~10-11 s por fotograma. Ahora el runner usa `LatestFrameSource` + worker (`_OCRWorker`) y el bucle solo dibuja (patrón de la app facial, ver `docs/history/stage-23-ocr-latency.md`). Además: recorte de ROI reducido a `max_width` (640), `canvas_size`/`mag_ratio` acotados, fotograma reducido a `max_frame_width` (1280, útil con la cámara del teléfono), y detección de cambios (`change_threshold`) para no reprocesar una ROI estática. Medido: ~3.6 s por inferencia (antes ~11 s); display a ~26-29 FPS con worker pesado incluso a 4K. Config ampliada en `config.yaml`. Tests: 37 casos (1572 passed, 93.96%).
- Fix latencia cámara de red (22/23): somnolencia, edad y género y desenfoque de privacidad corrían la inferencia en el hilo del bucle → con la cámara del teléfono (WiFi) el stream no se drenaba y el retraso crecía. Ahora usan `LatestFrameSource` + el worker genérico `cli/inference_worker.py` (`LatestInferenceWorker`): el bucle solo dibuja. Medido en headless: privacidad 32.7 FPS, edad/género 28.0, somnolencia 30.1 (antes ~2-5 FPS). Tests del worker genérico + runners actualizados (1575 passed, 93.97%).
- Menú por secciones: principal = gestos, contador de personas, anti-intrusos, facial, zona permanencia y zona vacía; al final "Otras apps" abre un submenú con las secundarias y las no creadas. `AppGroup` (`MAIN`/`OTHER`) en `core/domain/app.py` (`apps_in_group`); GUI (`build_menu_rows(group=...)` + `run_other_apps_submenu` como Toplevel) y consola (`render_catalog(group=...)` + submenú en `run_menu`) agrupan. Gate verde (1581 passed, 93.97%).
- Pendiente: la latencia con la cámara del teléfono (WiFi) sigue sin resolverse del todo en OCR/somnolencia/privacidad/edad-género (el usuario lo dejó pendiente).

## Siguiente (roadmap `docs/ROADMAP.md`)
- 25: EPP o inventario (menor prioridad 13 EPP y 14 inventario).
- Calibrar `ocr_reader.*` con cámara real: `roi_*` (ROI más chica = más rápido y mejor detalle), `max_width` (bajarlo acelera, subirlo detecta texto chico), `change_threshold`, `min_confidence`, `languages`.
- Si EasyOCR sigue lento en CPU: evaluar un adaptador RapidOCR/PP-OCR (onnxruntime ya está) — el puerto `OCREngine` permite intercambiarlo.

## Bloqueos / notas
- Verificación manual del usuario: diálogo de clave, `.exe` v0.2.0, ESC/q+X y aspecto visual del menú 10d.
- Calibrar con cámara real: `face_auth.min_face_width_ratio`/`match_threshold`, selector con 2 cámaras, `posture.*`, `anti_intruder.zone`, `people_counter.line`, `min_confidence`, `swap_handedness`.
- Patentes sin soportar aún: hace falta un detector de patentes propio (no hay modelo preentrenado).
- Calibrar `privacy_blur.*` con cámara real (`det_size`, `min_confidence`, `blur_strength`); coste CPU (~4.9 FPS en la prueba).
- Calibrar `gender_age.*` (`det_size`, `min_confidence`, `smoothing_window`) con cámara real; riesgo `FaceBox` compartido con etapa 19 (caja fuera del cuadro en un eje podría colapsar con `_clamp01`); smoke con cámara real pendiente.
- "Cabeza adelante" solo mide desvío horizontal en 2D (limitación conocida).
- Tras editar `opencode.json`/agentes/skills/comandos: reiniciar opencode.
