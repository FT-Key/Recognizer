# Modelos

Los archivos `.task` de MediaPipe son binarios y no se versionan.
Descargalos con:

```powershell
uv run python scripts/download_models.py
```

Catalogo actual: `hand_landmarker`, `gesture_recognizer` y `blaze_face_short_range`
(se usan en las etapas 1 a 3 del roadmap).
